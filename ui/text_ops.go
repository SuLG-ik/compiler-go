package ui

func (cw *CompilerWindow) doTask() {
	showInfoDialog(
		Strings.Actions.Task,
		"Раздел «Постановка задачи» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будет описана цель работы, требования к языковому процессору и "+
			"формальное описание решаемой задачи.",
		cw.win,
	)
}

func (cw *CompilerWindow) doGrammar() {
	showInfoDialog(
		Strings.Actions.Grammar,
		"Раздел «Грамматика» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будет представлена формальная грамматика в виде продукций (BNF / EBNF).",
		cw.win,
	)
}

func (cw *CompilerWindow) doGrammarClass() {
	showInfoDialog(
		Strings.Actions.Class,
		"Раздел «Классификация грамматики» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будет описана принадлежность грамматики к одному из классов "+
			"по Хомскому и обоснование этой классификации.",
		cw.win,
	)
}

func (cw *CompilerWindow) doMethod() {
	showInfoDialog(
		Strings.Actions.Method,
		"Раздел «Метод анализа» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будет описан алгоритм синтаксического анализа, "+
			"применяемый в языковом процессоре.",
		cw.win,
	)
}

func (cw *CompilerWindow) doTestEx() {
	showInfoDialog(
		Strings.Actions.TestEx,
		"Раздел «Тестовый пример» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будут размещены тестовые входные данные и ожидаемые результаты "+
			"работы языкового процессора.",
		cw.win,
	)
}

func (cw *CompilerWindow) doRefs() {
	showInfoDialog(
		Strings.Actions.References,
		"Раздел «Список литературы» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будут представлены библиографические ссылки на использованные источники.",
		cw.win,
	)
}

func (cw *CompilerWindow) doSrcCode() {
	showInfoDialog(
		Strings.Actions.SourceCode,
		"Раздел «Исходный код программы» будет реализован в следующей лабораторной работе.\n\n"+
			"Здесь будет отображаться аннотированный исходный код языкового процессора.",
		cw.win,
	)
}
